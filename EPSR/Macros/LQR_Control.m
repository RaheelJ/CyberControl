%% LQR Runtime  
    % j=1 %%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%     
    if(r<=0.45)
        d=0.06*rand(1, 1);                  %Delay               
                 
    % j=2
    % %%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
    elseif(r>0.45 && r<=0.65)
        d=0.06+0.06*rand(1, 1);    
        
    % j=3 %%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%    
    elseif(r>0.65 && r<=0.75)
        d=0.12+0.06*rand(1, 1);
        
    % j=4 %%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%    
    elseif(r>0.75 && r<=0.85)
        d=0.18+0.24*rand(1, 1); 
        
    % j=5 %%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%    
    elseif(r>0.85 && r<=1)
        d=0.24+0.06*rand(1, 1);
    end
    
    if (d<0.2)
        
        ux(:, l)=-F*xx_(:, l);
        %System response due to delay
        run('LQR_Delayed_Response.m')

        %Normal system reponse
        run('LQR_Normal_Response.m')

        %Observer state updation
        xx_(:, l+1)=A*xx_(:, l)+B*ux(:, l)+L*C*(temp-xx_(:, l));
        temp=xx(:, KK-1);
    else
    
        ux(:, l)=-F*xx_(:, l);
        %System response due to delay
        d=T;
        run('LQR_Delayed_Response.m')

        %Observer state updation
        xx_(:, l+1)=xx_(:, l);
        temp=xx(:, KK-1);
    end